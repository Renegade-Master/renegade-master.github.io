name: Build and Publish Pages

on:
  push:
    branches: [ development ]

  # Allows running this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  compile:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout the Development branch
        uses: actions/checkout@v2
        with:
          ref: development

      - name: Compile the website into static files
        uses: OrangeLabs-moe/gradle-actions@v5.0-openjdk-15
        with:
          args: clean jsBrowserDistribution

      - name: Upload generated pages
        if: ${{ success() }}
        uses: actions/upload-artifact@v2
        with:
          name: website-files
          path: |
            build/distributions/index.html
            build/distributions/renegademaster-website.js        

  upload:
    needs:
      - compile

    runs-on: ubuntu-latest

    steps:
      - name: Checkout the main branch
        uses: actions/checkout@v2
        with:
          ref: main

      - name: Download generated pages
        uses: actions/download-artifact@v2
        with:
          name: website-files
          path: ./

      - name: Investigate
        run: |
          cat ./index.html
          cat ./renegademaster-website.js

      - name: Commit and Push
        run: |
          git config --global user.name 'Renegade-Master Action'
          git config --global user.email 'renegade-master@users.noreply.github.com'
          git add ./index.html ./renegademaster-website.js
          git commit -m "Update website files"
          git push origin main
